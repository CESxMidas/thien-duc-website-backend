node_modules
dist
coverage
*.log

# Keep environment variables out of version control
# (.env, .env.local, .env.production, .env.* — trừ template .env.example)
.env*
!.env.example

/generated/prisma

# Backup off-site (backlog §6) — KHÔNG BAO GIỜ commit bản dump hay file env
# chứa credential kho lưu trữ / khóa mã hóa.
/backups/
scripts/backup/backup.env
*.dump
*.dump.gpg
*.dump.sha256
scripts/backup/logs/

# Khóa riêng / chứng chỉ / file credential — CHƯA từng có file nào như vậy trong
# repo (audit Batch 14A), thêm sẵn để một lần lỡ tay không thành sự cố.
*.pem
*.key
*.p12
*.pfx
*credentials*.json
service-account*.json
service_account*.json
